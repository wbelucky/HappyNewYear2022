{
  "name": "pixi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cp -r resources dist && npx parcel src/index.html",
    "build": "cp -r resources docs/resources && npx parcel build src/index.html --out-dir docs --public-url . --no-source-maps"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "pixi.js": "^5.2.0",
    "typescript": "^3.7.4"
  }
}
