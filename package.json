{
  "name": "pixi",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "mkdir -p dist && cp -r resources dist && npx parcel src/index.html",
    "build": "rm -r docs; mkdir -p docs; cp -r resources docs && npx parcel build src/index.html --dist-dir docs --public-url . --no-source-maps"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.8.2",
    "punycode": "^1.4.1",
    "querystring-es3": "^0.2.1",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "pixi.js": "^7.0.5"
  }
}
